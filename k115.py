# With + return
def factor(x):
    # process
    f=1
    for i in range(1,n+1):
        f=f*i
    return f
    # input
n=int(input(" Enter number : "))
    # calling + display
f1=factor(n)
print(" Factorial number as = ",f1)
